# 评估明细表唯一模板来源

所有评估明细表填报流程以用户维护的 `assets/builtin_templates/valuation-detail-workbook-fill/template.xlsx` 为唯一发布来源。不得用 skill、build、dist 中的旧副本反向覆盖它。Word 类 skill 不受此约定影响。

本轮用户更新的模板 SHA256：`d1e3e900ea547347e8643391f6c5698bd66aa1168a33dcd4f276e3a665806705`。

已将此文件逐字节同步到仓库 skill、build 暂存资源、当前 dist 客户端，并更新对应模板校验配置。输入文件未作内容、公式或格式修改；历史业务成果未改写。

模板再次更新时，应由维护者重新检查并同步所有副本与哈希，不允许自动忽略哈希校验。填报在项目副本中进行，不覆盖模板。该来源约定优先于历史文档中的模板哈希。

本次为模板资源同步，不代表未验收的脚本/客户端功能已全部发布；EXE 二进制、旧 ZIP、GitHub 与云端未在本次重新发布。
