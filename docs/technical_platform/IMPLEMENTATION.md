# ZQ 技术平台本地 Demo 实施记录

本清单记录实际完成状态，未通过验证不标记完成。

## 既定边界

- 项目、会话、文件版本、执行与成果为平台通用对象。
- 审核为首个 Skill，只读原件，允许生成独立成果。
- 写入原件权限默认关闭，未来修改 Skill 单独授权。
- 项目和用户记忆隔离，忽略问题不等于误判反馈。
- 公共 Skill 优化须测试、管理员审批、版本化和可回滚。
- 原文件不上传；隐藏表及污染依赖不得进入模型或记忆。

## 实施清单

- [x] T01 核对工程基线并确认可运行验证环境
- [x] T02 项目、会话与消息持久化（本地预览）
- [x] T03 三栏对话式桌面工作台（本地预览）
- [ ] T04 Skill 注册、权限声明与执行快照
- [ ] T05 审核适配、本地只读预检与成果浏览
- [ ] T06 服务端后台任务、取消与恢复
- [ ] T07 建议与多轮对话服务端化
- [ ] T08 分层记忆、反馈与优化审批回滚
- [ ] T09a 客户端首次改密、连接管理及余额刷新（已实现，真实联调待验收）
- [ ] T09b 独立 Web 总控服务台（首版已实现，管理闭环待完成）
- [ ] T10 本地 HTTPS/PostgreSQL/真实模型联调
- [ ] T11 新 EXE 构建与干净 Windows 验收
- [ ] T12 完整业务、安全、计费及恢复验收

## 环境记录

当前执行环境为 Linux/WSL，工作区在 `/mnt/d/1/1/ai-excel-agent`。
原 `.venv` 为 Windows 环境；已验证可从 WSL 直接调用 `.venv/Scripts/python.exe`，
因此复用原环境，不需要初始化 Linux 环境。当前根目录缺少历史
`pyproject.toml` 和 `CONTEXT.md`，启动及测试脚本显式设置源码搜索路径。

## 第一批实际结果

- 新增 `technical_platform` 包，未替换旧入口。
- T04：有注册表、只读权限声明、不可变任务输入快照及状态门禁；
  通用工具沙箱和服务端注册发布尚未完成。
- T05：已完成本地预检；正式审核有注入式远程适配器并通过模拟输入测试，
  新界面的认证入口和完整业务迁移尚未完成。
- T08：已完成项目偏好确认/删除、范围隔离、反馈类型检查；
  优化候选、审批、回放、发布和回滚尚未完成。
- 新测试 9 项通过；旧客户端审核回归 155 项通过。
- 新增代码 Ruff 检查通过；1440×900 离屏界面快照检查通过。
- Windows 离屏 Qt 未枚举系统字体导致中文方框，已显式加载微软雅黑修复。

## 下一批接续入口

1. 新工作台接入服务端认证、首次改密、连接监测与余额（第二批已实现，真实联调待完成）。
2. `review-jobs` 后台执行与任务恢复，并补充任务停止的明确语义。
3. 迁移问题卡片、建议对话、多轮复核与独立成果导出。
4. 项目文件副本/版本选择与归档恢复已在第二批实现，接续完善 Skill 权限执行。
5. 总控、长期记忆治理及 Skill 优化闭环。

第一批是可运行预览，不能标记 T06–T12 完成，也不能替代完整 Demo 验收。

## 第二批实际结果

- T09 部分完成：独立登录窗口、首次临时密码修改、改密后重新认证、
  服务端模型获取、登录/执行后/手动及心跳余额刷新。
- 联网模式使用服务端地址与账号ID隔离本地项目，refresh token通过
  Windows凭据管理器保存，客户端不配置供应商API Key。
- 后台心跳不占用界面线程；断网暂停新任务并在30秒期限到达后保存
  interrupted 检查点退出；会话失效立即退出。
- T05 部分完成：新增文件复制到所选目录内的项目附件版本目录，哈希校验；
  文件列表可勾选版本，默认选择同名文件的最新版本；项目可归档并恢复。
- 修复旧客户端认证JSON封装误判204空响应的兼容性缺陷。
- 新平台17项测试、旧认证和远程审核15项、旧桌面15项、
  旧客户端全量155项、服务端35项均通过（各组有重叠，不相加）。
- 首次旧客户端全量运行发生一次 Qt 原生访问冲突；独立桌面测试及随后
  全量重跑通过。尚未确认根因，记录为稳定性待观察项，不宣称已修复。

## 尚未达到完整验收的关键点

- T06 服务端仍使用同步 execute 请求；后台执行、持久恢复、停止供应商调用
  尚未完成。当前停止只在文件边界或响应返回后生效。
- T07 建议/多轮对话，T08优化审批/回放/发布/回滚，T09总控管理闭环未完成。
- T10本地HTTPS/PostgreSQL真实联调、T11安装包、T12完整验收仍未完成。
- T04通用工具沙箱及服务端强制执行权限仍待完成，不能据本地注册表宣称沙箱隔离。

## 第三批：独立 Web 总控首版

- 已接通管理员登录、账号创建/重置、余额调整、用户倍率、模型/渠道新增和概览。
- 客户端不增加管理员入口；总控与客户端分别验收。
- 服务端38项测试通过，包括总控权限、静态资源安全头及密钥不回显测试。
- 详细启动方式及未完成边界见 `CONTROL_DESK.md`；浏览器实测仍待完成。

## 客户端文件拖放

- 中间对话区（包含正文和输入框）接收本地多文件拖放，沿用只读附件副本流程。
- 支持 docx/xlsx/xlsm/pdf；拒绝文件夹和网络链接，不自动执行任务。
- 同名同内容跳过，同名不同内容保存新版本；混合输入逐项处理并汇总提示。
- 无项目或任务执行中禁止添加，原“＋”按钮复用同一处理流程。
- 测试先行：修复前2项失败，修复后新平台19项通过；尚未进行资源管理器实机拖放验收。
