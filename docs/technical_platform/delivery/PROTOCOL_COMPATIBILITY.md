# 协议兼容性实施记录

本文件记录当前实际能力，不代表G01全部验收通过。

计费待核对（本地新增，未部署）：缺失/无效usage、网络结果未知、无效JSON且无可信用量时，计费请求和冻结记录可为`uncertain`；请求返回409 `billing_reconciliation_required`，不自动重试或释放冻结。审核任务为failed并保留该专用error_code，不引导新建任务重试。该账号暂停新预占及已有冻结的新调用，已完成请求仍可幂等读取。管理员核对入口和客户端专用提示尚待补齐，不能仅上线此中间状态；升级后回退旧服务会不理解uncertain状态，发布前必须补相应兼容/回退门禁。

| 请求 | 执行前条件 | 不满足时 |
|---|---|---|
| skill-route | 已登录；schema_version=1、protocol_version=1且skill_routing=1 | 禁止业务POST，提示部署版本/能力问题 |
| agent/understand（本地新增，未部署） | 已登录；task_understanding=1；请求与返回均按共享Understanding协议校验 | 禁止退回猜测任务，不执行业务；咨询允许无附件，plan必须有已提供的目标与Skill |
| agent/plan（本地新增，未部署） | 已登录；task_planning=1；仅完整execute/plan理解可请求；PlanningRequest/PlanProposal schema 1，客户端和服务端共同校验本轮范围/角色/依赖 | 不编译不执行业务；提案调用按plan:<request_id>幂等计量，模型已调用时校验失败不代表免费 |
| review-jobs创建 | 已登录；同上且review_jobs=1 | 不创建任务、不预占本轮额度 |
| material-analysis | 已登录；同上且material_analysis=1 | 不发送业务资料片段 |
| 旧服务端无capabilities（404） | 只读检查openapi.json中该路径存在POST操作 | 缺失、格式错误、请求失败均不发业务POST |
| 新服务端声明未来schema/protocol | 不兼容，不尝试降级成旧协议 | 禁止业务POST |

兼容性检查不等于模型渠道可用、余额充足或任务授权；这些门禁必须独立继续执行。旧OpenAPI检查只能证明接口存在，不能证明新增语义行为被实现。

GET预检不附带模型输入、账号密码或供应商Key。构建号来自部署明确配置的REPORT_REVIEW_BUILD_SHA；未配置返回null，不能以API版本替代。

任务路由在预检前后检查取消令牌；已经提交的模型调用不承诺退款或服务器撤销。资料识别、执行中流式请求、Office步骤和云端任务取消仍需G03完整验收。

本地版本详情现列出全部BUILTINS的适配器版本；生成类Skill复用实际执行的locked_template校验，再计算模板SHA256和技能包指纹。资源缺失/变更记为unavailable_or_changed，不宣称已验证。该信息只在本地版本面板展示，不上传项目路径或凭据。local_schema_version表示客户端支持的版本，不证明任一项目已经迁移。外部安装Skill的版本与兼容性仍待统一目录接入。

外部Skill现进入版本面板：按当前账号列出包版本、清单schema、适配器和实际包哈希；重新校验依赖与完整性，区分未启用/依赖缺失/已启用且包兼容/格式或完整性异常。仅只读查询，未初始化安装表时明确无法确认；不创建表、不显示规则正文、不替用户启用。该结果是本地检查时的快照，不替代执行前再次校验和具体动作授权，也不是未来新增能力契约的完整兼容矩阵。

TaskIdentity已进入真实TaskSpec与执行入口。当前本地新任务为TaskSpec v2；任务身份及ExecutionPlan协议独立保持schema 1。v0/v1历史仍可读，但不恢复执行授权，需重新提交；v2在执行前必须校验持久确认记录、计划和文件范围。

本地数据库当前支持schema 6：schema 1对话状态、schema 2计划/步骤/事件、schema 3授权记录、schema 4逐步骤结果、schema 5会话归档/已读/分叉来源元数据、schema 6独立会话草稿。schema 5/6新增独立表，不改变旧sessions列；来源快照和草稿附件选择不代表执行授权。旧库迁移前备份，未来版本拒绝打开；活动任务尚未解决时阻止迁移，不能用升级程序盲目重跑远端调用。旧客户端不能打开新版库，降级必须使用升级前备份，不能仅替换EXE。合成v4库及v5到v6迁移故障备份/事务回滚已测，真实历史升级链尚未验收。

顺序调度、事件及结果引用已完成本地核心测试；真实只读审核和生成单步骤适配器及客户端入口已接通，继续向原UI提供review/preflight/generation结果格式。只读审核幂等标识固定到任务及步骤；生成当前限定原有单execute步骤及独立任务目录，不开放重复使用目录。Artifact解析已支持生成主成果到只读步骤，核对成功祖先、结果归属、目录及哈希；手工DAG真实生成/解析测试通过，不等于自然语言组合任务已接通。

新增独立PlanProposal schema 1，不改变已有Understanding接口响应：模型只提步骤目标、Skill引用、文件/前序产物引用及角色/依赖，不提供代码路径、版本或授权。compile_proposal绑定本轮request_id、理解范围及本地能力/规则哈希，保留每步goal/constraints/target_inputs/reference_inputs，拒绝遗漏范围、格式不支持及非文件成果当文件。ExecutionStep新增字段均可缺省；原字段未变化，旧计划经模型规范化比较，不改写原授权绑定快照。该编译器尚未接服务端模型响应和UI，参考角色及步骤目标向业务适配器的传递仍需后续验证，不能直接宣称可用。自然语言组合计划入口、细粒度权限、远端取消/事件续接、外部Skill能力兼容、升级回退矩阵及实际构建SHA仍待完成。

补充：服务端提案接口与RemoteSessionClient.propose_plan现已实现并通过模拟测试，共享计划验证也接本地编译器。尚未在UI主动调用，也未部署至Zeabur，不能将接口单测视作客户端多Skill实际验收。Dockerfile复制完整服务端目录（含新prompt）和共享agent_contracts；部署平台自定义Dockerfile覆盖仍须发布时核实。

步骤参数补充：ReviewAdapter现传递步骤目标/约束及精确参考ID。参考片段在原角色隐私过滤完成后再标reference_only；只向步骤目标输出问题，参考/范围外意见被排除时记录用户可见警告。无目标的纯参考审核拒绝调用模型。旧步骤缺省角色时保留原输入为目标（PDF仍仅参考）。组合理解仅做能力合集格式初筛，真正的逐步骤分配必须由compiler校验；UI组合入口仍未接通。

生成步骤补充：compound计划可通过step_configs绑定各生成步骤的规则指纹、输入角色和自动识别模式。每步使用runs/<task>/steps/<sha256(step_id)>目录，原有单任务仍使用runs/<task>；目录不得重复使用或重定向。双步骤真实合成工商文档生成及指定成果解析已测，跨步骤返回他步骤成果拒绝发布。上述是执行器能力，尚需通用TaskSpec构造、UI授权入口和复合成果交付，不能宣称自然语言多任务已可用。

组合TaskSpec补充：build_compound_task_spec与execute_task现支持mode=compound，保存理解请求/提案、范围修订、规划时文件快照、逐步骤本地配置；执行前从当前内置规则重编译比对。模型步骤要求client登录，分别建立provider；本地工商单一Excel来源自动映射，不再要求计划调用者手改角色。通过真实生成到预检及真实生成到模拟审核验证，未使用收费模型。外部包的组合规则绑定尚未接入会明确拒绝；UI确认与复合成果交付仍未实现。

后续进展（替代上文对应“UI未接入”的历史状态）：客户端UnderstandingWorker已请求并验证组合提案，使用纯文本计划确认窗口绑定范围、版本、账号、会话、模型及修订后执行。取消、模型/修订变化、迟到响应不创建业务任务。复合结果在对话按步骤交付，生成成果链接重验归属、目录及哈希；完成多步骤可以只读核对恢复成功状态，不重跑。674项完整回归通过，但理解/计划及模型审核使用模拟响应，尚未部署真实提案服务或执行收费验收。

复合审核步骤可单独导出标准Word审核报告：review_delivery视图从不可变步骤结果和注册输入解析准确送审范围；导出元数据写入父任务review_deliveries，不修改步骤检查点。保存位置由用户选择并禁止系统盘，已有文件/原件不覆盖。逐步骤批注主动询问及副本交付、单步骤compound结果兼容仍待补验收，不能称所有审核后续动作已经接通。PDF目标限制已收窄到正式report.review，本地可读取性预检仍可处理PDF。

后续补齐：逐步骤批注已接入主动询问、允许/拒绝、原件不变的副本执行及对话链接；询问状态与副本清单仍只写review_deliveries。新compound任务即使只有一步也保持plan结果封装，旧非compound单步骤返回格式不变。678项完整回归通过，含真实合成文档副本，但不等于Office/WPS实机矩阵通过。

外部规则组合现已接入：step_configs可附external_skill的id/version/SHA256，规则正文为内置约束加已启用数据包指令，编译和执行前均按当前账号重新加载完整性/依赖并比对。该绑定不开放脚本执行、模板替换或原件修改；正在排队/执行/待核对的组合任务会阻止切换所用包。适配器ID/版本仍来自内置可信目录，外部包身份另列于确认页。684项完整回归通过，含对话→真实生成→模拟外部规则审核；线上理解/规划、语义留出集及发布链仍未验收。

澄清消息保真修复：MessageRef单条上限扩大到12000字，与当前用户要求一致；UnderstandingRequest仍保持10条上下文/64KB总限。客户端不再将消息裁到2000字或静默删掉早期约束；保存下一次追问之前预验证可续接大小，超界明确提示完整重述，现有状态不变，不执行业务。新长上下文需配套更新服务端，旧服务端若拒绝不能通过截断重试规避；部署兼容验收尚未完成。该修复687回归通过；后续加入离线标签评分基础后的完整回归692通过，不代表真实语义验收。
