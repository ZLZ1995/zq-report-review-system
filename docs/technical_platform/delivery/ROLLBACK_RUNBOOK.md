# 回退约束（待演练）

尚未执行迁移或发布。以下不是已验证的回退能力。

1. 保留旧稳定包、签名清单、schema及构建SHA，不覆盖原产物。
2. 本地迁移先在副本验证并备份，原业务目录不删除。
3. 云端部署前确认一致性备份可恢复；保留JWT与供应商加密密钥。
4. 新版本异常先撤回更新通道，保持任务状态查询与计费核对；不得重跑不明远端写入。
5. schema不兼容不直接降EXE或云端库；恢复演练在隔离数据上做，不静默丢弃新写入。
6. 同机更新保留本地加密凭据，不导出明文或上传云端。

实际命令/版本/恢复时间在G08—G10有验证证据后补齐。

客户端schema 5新增session_metadata；合成v4库迁移先生成原版本备份，注入失败时新增表及版本号事务回滚已测试。成功迁移后不能直接用schema 4旧程序打开，亦不能删除元数据表作为生产降级手段。实际更新器的备份恢复、迁移后新增会话保留及跨版本回退仍待G08验证。

本地新增0004_billing_reconciliation：新增费用核对审计表，不修改既有账号/余额。空表可降回0003；存在审计记录时迁移主动拒绝删除，必须保留前向兼容服务或经独立备份恢复方案处理，不能强行删表。该门禁仅已用SQLite合成数据验证，生产PostgreSQL备份恢复和并发结算尚未验收。
