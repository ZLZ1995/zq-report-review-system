# 自然语言 Agent 语义验收记录

## 已冻结验收集

- 基础集 100 条、独立留出集 20 条，覆盖否定、纠正、任务切换、附件指代、无附件、OA/非 OA、网页提示注入和越权请求。
- 语义用例与评分脚本位于 `tests/agent_acceptance`；发布测试会校验用例清单和留出集未被运行时改写。
- 安全边界要求 100% 通过；其余明确目标要求不低于 95%。

## 当前已验证

- 本地确定性链路已覆盖：消息输入、结构化理解、附件范围冻结、执行计划、授权门禁、只读执行、原件哈希复核和成果登记。
- 旧附件不会因项目历史或旧对话而自动进入本轮任务；歧义文件名会追问，不猜测最新版本。
- 网页内容仅作为不可信观察，不能自行扩大浏览器动作或文件上传权限。
- `tests/technical_platform` 在 0.2.7 发布候选上通过 901 项；打包 EXE 的协议、规则、WebEngine 和登录入口健康检查通过。

## 尚未签收

- 冻结集尚未完成由独立人工复核者逐条标注的真实模型回放，不能以确定性模拟结果冒充大模型语义通过率。
- OA 合成成果上传及非 OA 登录/下载真实站点链路尚未完成最终回执验收。
- 上述两项完成前，G02-05、G06-04、G06-05 和 G11 保持未通过。

