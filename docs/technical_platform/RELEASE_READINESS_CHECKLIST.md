# 完整可用版本工作清单

更新日期：2026-09-15。当前已发布版本：0.2.3。本清单各项必须有验证证据，不能以代码已写代替验收。

## A. 模板与业务生成（发布阻塞）

- [x] A01 明确 Office 或 WPS 任一种可用；独立只读重算、保护用户已打开文件。已有兼容回归与两种后端小样本实测。
- [x] A02 工商模板绑定及适配：正文小四、表格小五，首行缩进分别 2 字符和 0；既有生成与分页验证。
- [ ] A03 构造可重复的明细表完整来源样例，用批准的真实模板跑完来源解析、填充、重算和复核。
- [ ] A04 对复现的阻塞逐项先补失败测试，再做最小修复；不能放宽来源或发布门禁。
- [ ] A05 Excel、WPS 分别完整生成；重读原来源核对金额、方向、单位、期间、客商和漏项。
- [ ] A06 对比全部既有公式、链接、工作表与固定结构；修改测试副本明细值后验证汇总联动。
- [ ] A07 建立明细表模板锁定清单及哈希；篡改、缺模板、缺组件均明确拒绝执行。
- [ ] A08 明确受支持的来源格式；缺资料、格式不支持、数据不平衡必须提供可读反馈，不交付伪成果。

## B. 整体回归

- [ ] B01 运行技术平台、原审核客户端、服务端、两项生成相关回归并保存结果。
- [ ] B02 验证任务取消、异常恢复、原文件不变、路径隔离、成果链接及非系统盘存储。
- [ ] B03 更新技能和产品说明，消除旧版计算路线与实际实现冲突；标注支持边界。

## C. 客户端交付

- [ ] C01 升级版本、检查打包资源清单，纳入全部本轮系统修改，不混入客户资料或密钥。
- [ ] C02 生成 EXE；用冻结程序执行工商和明细表样例，检查模板、脚本、依赖齐全。
- [ ] C03 EXE 登录启动及成果打开实测；计算包哈希、生成发行说明、保留上一版回退包。

## D. 发布与云端联调

- [ ] D01 按明确白名单核对 GitHub 差异，提交并推送；保留远端其他改动。
- [ ] D02 确认 Zeabur 部署对应提交成功、健康接口正常，不改动现有账户和计费数据。
- [ ] D03 新客户端与服务端接口兼容复核；需真实付费模型调用时明确测试范围与结果。
- [ ] D04 最终复查所有未通过项，交付版本、位置、验证记录及已知限制。

## 执行记录

- 2026-09-15：建立清单；从 A03 完整模板验证开始。不把原先 76 项明细表测试、110 项平台测试视为完整模板验收。
- A03 已建立 `scripts/probe_detail_generation.py`：合成且平衡的来源 + 批准的完整模板；输入及模板哈希前后校验，不跳过门禁。当前样例缺序时账，属于明确失败分支，成功分支仍需补充。
- 已复现并修复：映射脚本向上递归寻找未授权客商资料；阶段一保存后未重算就检查公式合计；后处理覆盖资产负债表公式。每项有先失败后通过的测试。
- 新增全工作簿公式保护和有界批量读取计算缓存，Excel/WPS 小样本均复测通过。新增及邻近测试合计 137 项通过（测试集有重叠，不与旧记录直接相加）。
- A06 部分验证：阶段一的 11,456 条模板公式、显式链接和表顺序均保留；阶段二旧页脚/明细写入改变多处公式，尚未通过。`scripts/verify_detail_template_preservation.py` 可重复只读对比；没有用恢复数值或改公式规避门禁。
- 完整样例 `outputs/detail_generation_acceptance/run_j83cpxcb` 已运行到最终复核并被阻断，未发布成果；缺分录日期、分类差异和模板公式变动仍须处理。模板与来源未改变。
- C/D 发布步骤尚未执行：不将存在上述阻塞的版本打包、推送或部署。
- 原审核客户端回归 164 项、服务端回归 66 项通过。完整模板新保护实测 `run__9no262q` 在保存前阻止 `应收账款!P24` 从 `=SUM(P6:P23)` 变为 `=SUM(P6:P6)`，确认门禁生效，来源与模板原件哈希不变。后续需修正明细写入的合计范围重建、旧页脚归并及负债公式格放开写入逻辑，而不是撤销此门禁。

### 2026-09-15 第二轮逻辑修复

- 锁定模板路径不再归并/重建页脚，不缩短合计公式，不放开派生公式格；只清理注册输入区，容量不足写入前阻断。原脚本非锁定辅助路径保留，避免误伤已有测试工作流。
- 不再通过应收总额与报表差额推造坏账准备；锁定路径将未能勾稽的证据交给复核处理。
- 账龄使用本轮报表日期，未来发生日期阻断；序时账两个同名客商字段独立提取去重，冲突主体阻断。
- 新增失败测试后修复：固定布局三项、项目账龄、重复/冲突客商；综合平台及邻近回归 171 项通过，明细旧流程邻近回归另已通过。
- Excel 完整成功样例：`outputs/detail_generation_acceptance/run_5t7pcx_h`，来源为合成应收/应付各 1000 元、平衡报表及对应序时账；完整批准模板实际生成，`completion_status=complete`、`unresolved_count=0`、`J4=OK`、报表差异 0，11,456 条原公式和显式链接、表顺序全部保留，模板及来源哈希不变。此证据仅覆盖该样例，不表示所有来源布局、科目和版本已覆盖。
- WPS 完整同样例 `outputs/detail_generation_acceptance/run_5fpwo3f1` 已成功生成，退出码 0，来源及模板不变。EXE、模板锁定绑定及发布步骤仍未完成。
