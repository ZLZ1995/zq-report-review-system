# PI Agent 重构执行账本 S38

## 问题

模型上下文虽然列出了历史资料，但默认行为规则仍容易把“未在 UI 勾选”误判为“不可用”，导致用户补充文件或引用历史文件时被要求重新上传，Agent 没有先使用只读文件工具完成自然语言指代解析。

## 修改

- `ContextBuilder` 增加文件发现规则：涉及上传、补充、近期或历史资料时，先调用 `inspect_project_files`，再按需要调用 `analyze_file_roles` 和 `read_project_file`。
- 明确 UI 勾选不是只读分析的必要条件；但未绑定的历史文件禁止作为生成/修改目标，输出仍须显式目标绑定和策略批准。
- 增加规则注入回归测试。

## 验收

- ContextBuilder：23 passed。
- Agent rebuild 全量：400 passed, 9 xfailed，耗时 53.07s。

## 边界

- 该阶段只调整模型上下文与工具选择指引，不绕过文件范围、权限或原件只读保护。
- 真实模型服务仍需验证是否按规则自动调用盘点与读取工具；若模型协议错误，应进入协议兼容阶段处理。
