# 报告审核规则同步记录

同步来源：仓库 valuation-report-review-edit 的只读审核判断规则及审核后标注规则。

已更新运行时 review_rules.txt，并同步到 dist/technical_platform/ZQ技术平台/_internal/asset_based_agent/technical_platform/review_rules.txt。两者 SHA256 均为 781c1bc7d3206c4abe77bb36fbc1be7a7af6cb2b1a3d16adbb99f191f59ce82e。旧规则备份在 outputs/review_skill_sync_20260915/review_rules.before.txt。

新增边界：用户已确认暂空事项；内部模板与事实错误分离；本轮验证限制不冒充错误；导出显示异常须注明环境并具有视觉证据；最小建议校验；旧清单保留、降级、撤回及新增；局部批注简洁且保留不确定性。保持现有 JSON 协议、原件只读、隐藏表格排除和付费调用权限不变。

验证：目标运行链路 23 项通过，其中模拟服务端验证新版规则及本轮用户确认随请求传递；规则文件同步后相邻 15 项通过，集合有重叠。未调用付费模型，以上不代表实际审核准确率验收。

发布边界：本地已打包目录采用独立规则资源更新，EXE 二进制和旧 ZIP 未重建，云端和 GitHub 未改动。源码审核 Skill 版本为 0.2.0；原 EXE 编译内的版本显示仍可能为旧值，以运行时规则哈希为准。重启该目录的程序、新建审核任务后使用新规则；历史任务的规则快照不改写。其他目录安装的软件不会自动同步。

批注行为本次只同步规则；未新增批注执行器，不宣称已有批注文件生成或视觉验收能力。明细表 Skill 的完整验证和统一重新打包仍独立跟进，不因本次规则同步视为完成。
