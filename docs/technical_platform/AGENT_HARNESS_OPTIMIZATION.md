# Agent / Harness 分轮优化记录

每个小轮先补失败测试、确认红灯、最小实现、目标及邻近回归，再进入下一轮。
阶段不等于小轮；不得将前三个修复小轮表述为六阶段全部完成。

## 第一阶段：关键断点

- 第一轮：用户要求经 UI → 快照 → RemoteReviewLlm → ReviewJob → 每批模型提示传递。
  已验证：平台及适配器 41 项、服务端审核 11 项通过。
  旧请求字段缺省兼容；新增要求计入请求上下文与额度估算。
- 第二轮：启动账号 owner、切换账号重建 scoped store，清除旧会话、草稿及关闭旧 HTTP 客户端。
  已验证：平台 32 项、旧客户端 161 项、服务端 66 项通过。
  旧共享数据保留，不自动归属。应用层隔离不是磁盘加密。
- 第三轮：启动页显式离线入口；离线工作区与认证账号分离，模型调用仍须登录。
  平台 34 项通过。一次旧客户端完整回归发生 Python/Qt Aborted；详细重跑 161 项通过。
  该异常尚未稳定复现、不能宣称根因已修复。后续优化前需继续关注运行稳定性。
  最终完整脚本复跑：客户端 161 项、服务端 66 项通过；加平台 34 项，共 261 项。
  相关 Python 文件 compileall 校验通过。保留首次异常记录，不以重复通过消除风险记录。

## 尚未完成

### Qt 稳定性修复补充

旧配置窗口模型发现测试重复运行已复现访问违规及 Aborted。
确认测试在原生线程清理前即可返回；结果槽提前解除忙碌状态，窗口关闭亦无线程保护。
修复将结果缓存到线程 finished 后发布，wait 确认原生退出，再释放线程引用及恢复表单。
连接中关闭改为忽略当前关闭事件、待线程退出后自动关闭，不终止线程或阻塞网络等待。
测试增加生命周期清理 fixture 及三项成功/失败/关闭回归；三项修复前失败、修复后通过。
验证：窗口测试 13 项连续 15 轮通过；绕过保留引用 fixture 直接执行原故障测试 50 次通过。
完整回归：平台 34、旧客户端 164、服务端 66，共 264 项通过。
未更改审核业务、云端部署或 EXE；以上覆盖已复现竞态，并非宣称所有原生崩溃均已排除。
静态检查发现本次新增的导入顺序及闭包提示并修正；文件中四项既有提示未扩大范围处理。

1. 第一阶段剩余：旧项目认领/导入、产品版本展示和发布一致性、真实模型遵循要求的验收。
2. 第二阶段：统一 TaskSpec、历史与记忆正文筛选注入、Agent/Harness 适配。
3. 第三阶段：Skill 注册/安装/版本/依赖、工具级能力授权。
4. 第四阶段：证据定位、确定性校验、跨批次核查、多轮问题生命周期。
5. 第五阶段：问题对话、事件流、成果登记、经确认的 Word/Excel 批注副本执行。
6. 第六阶段：持久任务恢复/取消、记忆提案评测与确认回滚。

## 第一阶段收尾进度补充

- 已实现：显式旧共享项目认领；仅允许 local-preview 项目，要求确认、拒绝活动任务和他人账号项目。
  所有归属变更和认领记录在同一 SQLite 事务中提交；项目 ID、会话、附件、记忆关系保留。
  只修改测试构造数据，未对真实旧项目执行认领。
- 已实现：客户端 0.2.1 版本展示、Skill 版本、规则 SHA256；任务快照保存本地版本信息。
  服务版本按钮在后台检查 OpenAPI 协议；如不支持 user_request 则明确显示不兼容。
- 已验证线上：API 版本 0.1.0，user_request 字段支持。
  线上未提供构建号，界面明确标注未提供，不能将该项视为部署版本识别已完整完成。
- 本轮回归：平台 41 项、旧客户端 164 项、服务端 66 项，共 271 项通过。
- 已完成 0.2.1 EXE 打包；启动冒烟通过：实际 EXE 打开登录窗口，包内审核规则与源码一致。
  EXE SHA256：45f7f7950c54baddd2eca3a8017d0091c9ea7c3858b52cb37b4bd7d424c3e4bf。
  旧版完整目录保留在 dist/technical_platform-before-0.2.1，便于回退。
- 剩余验收：测试账号登录后的端到端实机及真实模型范围遵循验证；服务端构建标识。
  已请求测试账号及费用上限，未收到前不运行付费审核、不上传正式文件。
- 第一阶段尚未全验收，不越过用户要求的验证门禁进入第二阶段实现。
  第二阶段准备入口见 PHASE_TWO_CONTRACT.md。

此前 a2ffee56 服务端提交已在 GitHub main 与 Zeabur 上线；此处新增客户端收尾代码已重新打包为 0.2.1。

## 发布及验收边界

本轮新增的是本地认领和版本检查，未变更已部署的服务端；无真实付费模型调用，无正式文档修改。
此前新增 user_request 字段已在 a2ffee56 服务端部署，新客户端与该协议兼容。
自动化回归不能代替 EXE + 实际服务端 + 真实文件的产品验收。

## 第二阶段本地基础接入（2026-09-14）

用户最新指令“开始第二阶段”后进入实现；第一阶段剩余验收仍未销项。
完成 TaskSpec v1、有限历史/显式项目记忆上下文、固定只读 ExecutionHarness 三小轮接入。
各小轮先新增失败测试（模块缺失），再实现并跑平台回归：45 → 48 → 52 项通过。
随后补充 12000 字符预算、模型错配、账号隔离及执行后哈希验收回归。
最终平台 56、客户端 164、服务端 66 项，共 286 项回归通过；新增模块及测试 Ruff 通过。
仅假模型与构造附件，无正式文件修改、付费调用或部署。
实现边界及待续能力见 PHASE_TWO_CONTRACT.md；当前 EXE 仍为第一阶段 0.2.1 包，
本轮源代码尚未重新打包，不能将新源码能力视为已经发布给客户端用户。

### 第二阶段继续加固

已完成原子任务领取和完整文本去重，避免重复执行以及长要求同前缀导致的历史误删。
平台 59 项、旧客户端 164 项、服务端 66 项，共 289 项回归通过；新增及相关模块静态检查通过。
仍为本地源码验证，未变更已发布 EXE 或云端服务，第一阶段真实审核验收继续保留。

### 失败诊断展示轮

第二阶段阶段化错误记录已接入用户对话，并给出固定安全建议和任务编号；
不展示底层异常全文，旧记录不虚构阶段，运行中的重复任务不误报失败。
平台 66、旧客户端 164、服务端 66，共 296 项回归通过，新增模块及测试静态检查通过。
未更新 EXE、未部署、未执行付费模型审核。

## 第三阶段启动

按用户要求跳转第三阶段，第二、三阶段完成后统一打包；第二阶段真实验收仍待完成。
第一轮实现数据型 Skill ZIP 检查：严格清单、适配器/权限声明、依赖范围、资源完整性，
拒绝越界路径、符号链接、重复文件、脚本和超限包。仅检查，不安装依赖、不执行包代码。
17 项新包测试通过；最终平台 83、旧客户端 164、服务端 66，共 313 项回归通过。
新增模块和测试 Ruff 通过。
后续安装/启用/版本回退、任务版本固定、能力授权及管理入口尚未实施。
详细设计和分轮状态见 PHASE_THREE_CONTRACT.md。本轮无打包、部署或付费调用。

### 第三阶段持久安装管理轮

管理服务已实现账号隔离、事务持久化、默认停用、不可覆盖版本、启用/停用/版本切回，
并在启用前重新检查依赖和包哈希；相关任务未结束时阻止切换。同包重复安装不重复记账。
5 项安装管理回归通过（先确认模块缺失失败），最终平台 88、旧客户端 164、服务端 66，
共 318 项回归通过；新增与修改的包管理模块及新测试 Ruff 通过。
仅测试构造包，未安装真实用户 Skill；管理 UI、运行接入及工具授权仍待实施。
按约定继续暂缓 EXE 打包和部署。

### 客户端外部 Skill 安装入口

用户允许客户端安装外部 Skill 后，已增加侧栏管理窗口，支持 ZIP 预览、确认安装、
启用/停用版本。确认默认否，元数据纯文本展示，确认后包内容变化则拒绝安装。
界面明确标注安装状态不等于已接入执行；本轮尚未扩展 TaskSpec/Harness 支持外部包。
平台 91、旧客户端 164、服务端 66，共 321 项回归通过；相关新增及修改模块静态检查通过。
只使用构造包测试，未安装真实用户包，未打包或部署。
