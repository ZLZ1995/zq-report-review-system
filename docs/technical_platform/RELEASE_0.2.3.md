# ZQ 技术平台 0.2.3

汇总 0.2.2 后尚未发布的客户端变动：项目位置索引与非系统盘业务目录、已有项目重新打开、两项内置生成路由和授权/模板保护框架、工商格式规范及测试。

打包包含工商历史变更和评估明细表的 skill 说明、脚本及清理模板候选。工商模板正文小四、首行 2 字符，表格全部小五、首行 0；Excel 保留 11456 个公式和 409 个显式链接，空白数字输入保留待填状态。

发布边界：模板以 candidate_not_runtime_bound 状态打包；尚未建立运行时 template.lock.json，工商固定模板填充区域适配尚未完成。因此本地生成入口仍会明确阻断，不宣称两个 skill 已完成业务验收。外部 skill 安装管理保留，外部代码执行未开放。真实付费模型端到端测试未在本次运行。

回归基线：平台 99 项、旧客户端 164 项、服务端 66 项、工商 7 项通过。EXE 使用目录分发，必须保留 `_internal`，不得仅复制 exe。发布 ZIP、清单及 SHA256 在 dist/technical_platform 下生成。

服务器部署沿用既有 Dockerfile、数据库及密钥；本轮不更换任何生产环境变量，不执行账号或余额变更。
