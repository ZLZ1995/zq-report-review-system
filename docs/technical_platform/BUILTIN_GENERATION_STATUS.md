# 两项生成 Skill 接入状态

## 最新状态：新版明细表 Skill 接入（未发布 EXE）

客户端源码中的明细表 Skill 已升至 0.2.0，新增标准 XLSX 银行对账单资料角色、先识别资产负债表范围再扫描模板、自动选择范围模式，以及 execution_scope.json 成果记录。已绑定批准模板，哈希为 bcf61be8f2acac2e0badfc3f5599958560cee41ac25fe94a66efeeb4c13f0763；不允许改写预设公式或链接。

普通应收/应付完整样例 run_3gvh2pnv 成功；银行轻量路径正在复核。范围识别、绑定和写入保护相关 18 项测试通过。一次综合回归 141 项通过，2 项办公软件集成测试失败，不能标记为全量通过。下列记录为历史过程，不表示当前已发布；现有 EXE 仍是 0.2.3。

2026-09-15 后续实施与验证见 `RELEASE_READINESS_CHECKLIST.md`。完整模板端到端已开始，复现旧明细/页脚逻辑改写公式，现已新增全工作簿保护并实测阻断。当前尚未完成完整成功分支，不可发布新 EXE。

用户确认：评估明细表及工商历史沿革使用固定模板；不能替换模板或重新设计版式。成果回填到对应模板副本，原模板只读。

已完成：新增两项本地生成路由、明确资料角色/生成授权、项目目录隔离、模板绑定及哈希校验门禁、成果链接基础。修正工商脚本的日期特例与无证据股权金额分配。

本轮推进（尚未发布新版 EXE）：工商已绑定格式修正版模板 SHA256 `71a0999732ed949f4e96c184deb5d790d3baffa0bcb31a3426c245b07114f4e6`，新增固定事件占位区填充适配；已验证普通变更、股权金额证据、超过五个事件、原件保护、包部件保留，以及真实任务子进程和对话成果链接。缺少日期不再静默跳过；事件段落与表格保持同页。

用户已确认支持安装 Office 或 WPS 的电脑，不要求两者同时安装。客户端已取消固定跳过重算，自动优先 Microsoft Excel，不可启动时尝试 WPS 表格自动化组件。两种后端均已使用合成工作簿实测公式值与缓存；不 Save/SaveAs 原件，不关闭已有用户工作簿，缺少组件时明确阻止交付。此结论不代表所有 Office/WPS 版本均已验证；完整锁定模板验收仍待完成，明细表仍未放开。

本轮已修复明细表来源哈希校验对 Python 3.11 `hashlib.file_digest` 的依赖，兼容当前 Python 3.10 EXE。目标复核测试 11 项通过；其他明细表回归此前 69 项通过，两个哈希兼容失败已重测通过。仍需完整模板端到端，不能以单元测试替代。

待完成：明细表计算路线及锁定绑定、真实模板明细表端到端和链接验收、冻结 EXE 生成器实测、更新 GitHub 与服务器。当前发布版本仍为 0.2.3。

Office/WPS 兼容回归：明细表与计算安全测试 76 项通过，技术平台测试 110 项通过（包含前述计算安全测试，不作简单相加）。WPS-only 实际重算复测通过；新增关闭失败仍清理独立实例、拒绝修改已有用户工作簿的保护测试。此轮尚未重新打包或推送。

历史配置不能自动作为授权：本轮只使用 assets/builtin_templates 中已清理模板，不扫描旧项目文件。工商平台路径不调用旧 CLI 的清空全文逻辑。

确认后步骤：只读识别填充区域及受保护结构 → 固定模板哈希绑定 → 副本回填 → 来源/数值/结构门禁 → Word 分页与 Excel 链接验收 → 程序回归 → 按用户要求打包。

程序业务文件与中间文件只能落在用户非系统盘项目目录；不使用通用模板作为降级替代；本地生成不调用模型，不上传资料。
